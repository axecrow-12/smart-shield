# --- Node backend ---
PORT=5050

# URL of the Python ML fraud-scoring service
ML_API_URL=http://127.0.0.1:8000
# How long to wait for the ML service before falling back to local rules (ms)
ML_TIMEOUT_MS=2500

# Auth: payment/fraud routes require a Firebase ID token unless this is 1.
# Keep 1 for the expo demo; set 0 (or remove) in production.
ALLOW_ANON_DEMO=1

# The QR code on the merchant terminal encodes a "scan to pay" link that a
# phone on the same Wi-Fi/hotspot can open. The server auto-detects your
# machine's LAN IP for this; if it picks the wrong network adapter (common
# on machines with VPN/virtualization software), set it explicitly, e.g.:
# PUBLIC_BASE_URL=http://192.168.1.23:5050

# --- Firebase ---
FIREBASE_PROJECT_ID=smartpay-shield

# DEMO MODE (recommended, no cloud credentials needed):
# run `firebase emulators:start --only firestore` in another terminal, then:
FIRESTORE_EMULATOR_HOST=localhost:8765
# Uncomment if you also run the Auth emulator:
# FIREBASE_AUTH_EMULATOR_HOST=localhost:9099

# CLOUD MODE: comment out the emulator lines above and either place
# serviceAccountKey.json at the repo root, or set:
# GOOGLE_APPLICATION_CREDENTIALS=C:\path\to\serviceAccountKey.json

# --- EcoCash Instant Payment (EIP) real sandbox ---
# Values below are the vendor's own published sandbox values (EIP API
# v3.1.1 doc) + this account's registered merchant identity — not secret,
# but kept in env rather than source per project convention.
ECOCASH_EIP_BASE_URL=https://payonline.ecocash.co.zw/ecocashGateway-preprod/payment/v1
ECOCASH_EIP_USERNAME=ecocash
ECOCASH_EIP_PASSWORD=mobiquity
ECOCASH_MERCHANT_CODE=8003
ECOCASH_MERCHANT_PIN=2222
ECOCASH_MERCHANT_NUMBER=789111401
ECOCASH_TERMINAL_ID=TERM-SMARTSHIELD-01
ECOCASH_SUPER_MERCHANT_NAME=SMARTPAYSHIELD
ECOCASH_MERCHANT_NAME=SmartPay Shield Demo
ECOCASH_LOCATION=Harare, Zimbabwe
# Optional: pre-fills the "EcoCash Sandbox (Live)" card's phone field with
# your own registered sandbox test MSISDN (format: 263771234567)
ECOCASH_TEST_MSISDN=
